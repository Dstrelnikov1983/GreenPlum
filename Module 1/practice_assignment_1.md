# Практическое задание №1: Оптимизация интернет-магазина

## Описание сценария

Вы - инженер данных в компании "TechStore", которая запускает новый интернет-магазин. Вам необходимо спроектировать и реализовать схему базы данных в GreenPlum для аналитических запросов.

## Бизнес-требования

- **Объем данных:** Ожидается 1 млн заказов в год
- **Продуктов:** ~50,000 SKU
- **Клиентов:** ~200,000 активных пользователей
- **Запросы:** Аналитические отчеты по продажам, популярным товарам, поведению клиентов
- **Производительность:** Отчеты должны выполняться < 5 секунд

## Часть 1: Проектирование схемы данных (30 минут)

### Задание 1.1: Создайте схему базы данных

Необходимо создать следующие таблицы:

```sql
-- Используйте эту базу данных
CREATE DATABASE techstore_analytics;
\c techstore_analytics
```

#### Таблица 1: Customers (Клиенты)

**Поля:**
- customer_id (уникальный идентификатор)
- email (email адрес)
- first_name, last_name (имя и фамилия)
- registration_date (дата регистрации)
- country (страна)
- city (город)
- customer_segment (сегмент: 'Premium', 'Regular', 'New')

**Вопросы для размышления:**
1. Какую стратегию распределения выбрать?
2. Нужны ли индексы?
3. Какой размер будет у этой таблицы?

**Подсказка:** Таблица небольшая (200k записей) и часто используется в JOIN.

#### Таблица 2: Products (Товары)

**Поля:**
- product_id (уникальный идентификатор)
- product_name (название)
- category (категория: 'Electronics', 'Clothing', 'Books', etc.)
- subcategory (подкатегория)
- brand (бренд)
- price (цена)
- cost (себестоимость)
- supplier_id (идентификатор поставщика)

**Вопросы:**
1. Будет ли эта таблица расти быстро?
2. Как часто она используется в JOIN?
3. Какая стратегия распределения оптимальна?

#### Таблица 3: Orders (Заказы)

**Поля:**
- order_id (уникальный идентификатор)
- customer_id (внешний ключ)
- order_date (дата заказа)
- order_status ('pending', 'processing', 'shipped', 'delivered', 'cancelled')
- total_amount (общая сумма)
- payment_method ('card', 'cash', 'transfer')
- shipping_country, shipping_city (адрес доставки)

**Вопросы:**
1. Это большая таблица (1М+ записей) - как лучше распределить?
2. По какому полю чаще всего делаются JOIN?
3. Нужно ли партиционирование?

#### Таблица 4: Order_Items (Позиции заказов)

**Поля:**
- order_item_id (уникальный идентификатор)
- order_id (внешний ключ на Orders)
- product_id (внешний ключ на Products)
- quantity (количество)
- unit_price (цена за единицу)
- discount_amount (размер скидки)

**Вопросы:**
1. Самая большая таблица (в среднем 3-5 товаров на заказ = 3-5М записей)
2. Как распределить для оптимального JOIN с Orders?
3. Как обеспечить совместное размещение с Orders?

### Задание 1.2: Реализация схемы

Создайте все 4 таблицы с оптимальными стратегиями распределения.

**Рекомендуемая структура:**

```sql
-- ВАША ЗАДАЧА: Заполнить DISTRIBUTED BY для каждой таблицы

-- Таблица клиентов
CREATE TABLE customers (
    customer_id INTEGER PRIMARY KEY,
    email VARCHAR(100) UNIQUE NOT NULL,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    registration_date DATE NOT NULL,
    country VARCHAR(50),
    city VARCHAR(100),
    customer_segment VARCHAR(20)
) DISTRIBUTED __________; -- ЗАПОЛНИТЕ!

-- Таблица товаров
CREATE TABLE products (
    product_id INTEGER PRIMARY KEY,
    product_name VARCHAR(200) NOT NULL,
    category VARCHAR(50),
    subcategory VARCHAR(50),
    brand VARCHAR(100),
    price NUMERIC(10,2),
    cost NUMERIC(10,2),
    supplier_id INTEGER
) DISTRIBUTED __________; -- ЗАПОЛНИТЕ!

-- Таблица заказов
CREATE TABLE orders (
    order_id BIGINT PRIMARY KEY,
    customer_id INTEGER NOT NULL,
    order_date DATE NOT NULL,
    order_status VARCHAR(20),
    total_amount NUMERIC(12,2),
    payment_method VARCHAR(20),
    shipping_country VARCHAR(50),
    shipping_city VARCHAR(100)
) DISTRIBUTED __________; -- ЗАПОЛНИТЕ!

-- Таблица позиций заказов
CREATE TABLE order_items (
    order_item_id BIGSERIAL,
    order_id BIGINT NOT NULL,
    product_id INTEGER NOT NULL,
    quantity INTEGER NOT NULL,
    unit_price NUMERIC(10,2) NOT NULL,
    discount_amount NUMERIC(10,2) DEFAULT 0
) DISTRIBUTED __________; -- ЗАПОЛНИТЕ!
```

## Часть 2: Загрузка тестовых данных (20 минут)

### Задание 2.1: Генерация данных

Используйте предоставленные скрипты для загрузки данных:

```sql
-- Загрузка клиентов (200,000 записей)
INSERT INTO customers (customer_id, email, first_name, last_name, registration_date, country, city, customer_segment)
SELECT 
    generate_series as customer_id,
    'customer' || generate_series || '@email.com' as email,
    'FirstName' || generate_series as first_name,
    'LastName' || generate_series as last_name,
    current_date - (random() * 1095)::integer as registration_date,
    CASE (random() * 9)::integer
        WHEN 0 THEN 'USA'
        WHEN 1 THEN 'UK'
        WHEN 2 THEN 'Germany'
        WHEN 3 THEN 'France'
        WHEN 4 THEN 'Russia'
        WHEN 5 THEN 'China'
        WHEN 6 THEN 'Japan'
        WHEN 7 THEN 'Brazil'
        WHEN 8 THEN 'India'
        ELSE 'Canada'
    END as country,
    'City' || (random() * 100)::integer as city,
    CASE (random() * 2)::integer
        WHEN 0 THEN 'Premium'
        WHEN 1 THEN 'Regular'
        ELSE 'New'
    END as customer_segment
FROM generate_series(1, 200000);

-- Загрузка товаров (50,000 записей)
INSERT INTO products (product_id, product_name, category, subcategory, brand, price, cost, supplier_id)
SELECT 
    generate_series as product_id,
    'Product ' || generate_series as product_name,
    CASE (random() * 5)::integer
        WHEN 0 THEN 'Electronics'
        WHEN 1 THEN 'Clothing'
        WHEN 2 THEN 'Books'
        WHEN 3 THEN 'Home & Garden'
        WHEN 4 THEN 'Sports'
        ELSE 'Toys'
    END as category,
    'Subcategory ' || (random() * 20)::integer as subcategory,
    'Brand ' || (random() * 100)::integer as brand,
    (random() * 1000 + 10)::numeric(10,2) as price,
    (random() * 500 + 5)::numeric(10,2) as cost,
    (random() * 1000)::integer as supplier_id
FROM generate_series(1, 50000);

-- Загрузка заказов (1,000,000 записей)
INSERT INTO orders (order_id, customer_id, order_date, order_status, total_amount, payment_method, shipping_country, shipping_city)
SELECT 
    generate_series as order_id,
    (random() * 199999 + 1)::integer as customer_id,
    current_date - (random() * 730)::integer as order_date,
    CASE (random() * 4)::integer
        WHEN 0 THEN 'pending'
        WHEN 1 THEN 'processing'
        WHEN 2 THEN 'shipped'
        WHEN 3 THEN 'delivered'
        ELSE 'cancelled'
    END as order_status,
    (random() * 2000 + 50)::numeric(12,2) as total_amount,
    CASE (random() * 2)::integer
        WHEN 0 THEN 'card'
        WHEN 1 THEN 'cash'
        ELSE 'transfer'
    END as payment_method,
    CASE (random() * 9)::integer
        WHEN 0 THEN 'USA'
        WHEN 1 THEN 'UK'
        WHEN 2 THEN 'Germany'
        WHEN 3 THEN 'France'
        WHEN 4 THEN 'Russia'
        WHEN 5 THEN 'China'
        WHEN 6 THEN 'Japan'
        WHEN 7 THEN 'Brazil'
        WHEN 8 THEN 'India'
        ELSE 'Canada'
    END as shipping_country,
    'City' || (random() * 100)::integer as shipping_city
FROM generate_series(1, 1000000);

-- Загрузка позиций заказов (в среднем 3 товара на заказ = 3,000,000 записей)
INSERT INTO order_items (order_id, product_id, quantity, unit_price, discount_amount)
SELECT 
    (random() * 999999 + 1)::bigint as order_id,
    (random() * 49999 + 1)::integer as product_id,
    (random() * 5 + 1)::integer as quantity,
    (random() * 1000 + 10)::numeric(10,2) as unit_price,
    (random() * 50)::numeric(10,2) as discount_amount
FROM generate_series(1, 3000000);
```

### Задание 2.2: Обновление статистики

После загрузки данных необходимо обновить статистику:

```sql
ANALYZE customers;
ANALYZE products;
ANALYZE orders;
ANALYZE order_items;
```

### Задание 2.3: Проверка распределения данных

Проверьте, как данные распределились по сегментам:

```sql
-- Используйте функцию из файла monitoring_diagnostics.sql
SELECT * FROM analyze_table_distribution('orders');
SELECT * FROM analyze_table_distribution('order_items');
```

**Вопросы:**
1. Данные распределены равномерно?
2. Есть ли значительный перекос (skew)?
3. Если есть - что можно сделать для улучшения?

## Часть 3: Аналитические запросы (30 минут)

### Задание 3.1: Базовая аналитика

Напишите и выполните следующие запросы:

#### Запрос 1: Топ-10 клиентов по общей сумме заказов

```sql
-- ВАША ЗАДАЧА: Написать запрос
-- Должен вернуть: customer_id, имя, email, общую сумму, количество заказов
-- Отсортировано по общей сумме по убыванию
-- Используйте EXPLAIN ANALYZE для анализа производительности
```

**Ожидаемая производительность:** < 2 секунд

#### Запрос 2: Продажи по категориям за последний год

```sql
-- ВАША ЗАДАЧА: Написать запрос
-- Должен вернуть: категория, количество проданных товаров, общая выручка, средний чек
-- Только за последние 365 дней
-- JOIN orders, order_items, products
```

**Ожидаемая производительность:** < 3 секунд

#### Запрос 3: RFM анализ клиентов

```sql
-- Recency, Frequency, Monetary анализ
-- ВАША ЗАДАЧА: Написать запрос, который вычисляет:
-- - Recency: дней с последнего заказа
-- - Frequency: количество заказов
-- - Monetary: общая сумма заказов
-- Для каждого клиента
```

### Задание 3.2: Анализ планов выполнения

Для каждого запроса из 3.1:

1. Запустите с EXPLAIN ANALYZE
2. Найдите в плане:
   - Redistribute Motion (если есть)
   - Gather Motion
   - Sequential Scan vs Index Scan
3. Запишите время выполнения

**Пример:**

```sql
EXPLAIN ANALYZE
SELECT ...
```

### Задание 3.3: Оптимизация запросов

Если какой-то запрос выполняется медленно:

1. Проанализируйте план выполнения
2. Определите узкие места
3. Предложите оптимизацию:
   - Изменить distribution key?
   - Добавить индекс?
   - Изменить запрос?

## Часть 4: Эксперимент с альтернативными стратегиями (20 минут)

### Задание 4.1: Создайте альтернативные версии таблиц

```sql
-- Версия 1: Orders с RANDOM distribution
CREATE TABLE orders_random AS 
SELECT * FROM orders 
DISTRIBUTED RANDOMLY;

-- Версия 2: Orders с другим distribution key
CREATE TABLE orders_alt AS 
SELECT * FROM orders 
DISTRIBUTED BY (order_date);
```

### Задание 4.2: Сравните производительность

Выполните один и тот же запрос на всех трех версиях:

```sql
-- На оригинальной таблице
EXPLAIN ANALYZE
SELECT 
    customer_id,
    count(*) as order_count,
    sum(total_amount) as total_spent
FROM orders
WHERE order_date >= current_date - 365
GROUP BY customer_id
ORDER BY total_spent DESC
LIMIT 100;

-- На orders_random
-- [ПОВТОРИТЕ ЗАПРОС]

-- На orders_alt
-- [ПОВТОРИТЕ ЗАПРОС]
```

**Заполните таблицу результатов:**

| Таблица | Distribution | Время (мс) | Redistribute Motion? |
|---------|--------------|------------|---------------------|
| orders | ? | ? | ? |
| orders_random | RANDOM | ? | ? |
| orders_alt | order_date | ? | ? |

### Задание 4.3: Выводы

Ответьте на вопросы:

1. Какая стратегия показала лучшую производительность?
2. Почему?
3. В каких сценариях другие стратегии могут быть лучше?

## Часть 5: Мониторинг и диагностика (10 минут)

### Задание 5.1: Проверьте здоровье кластера

```sql
-- Используйте скрипты из monitoring_diagnostics.sql

-- 1. Общее здоровье
SELECT * FROM daily_health_check();

-- 2. Размер таблиц
-- [ЗАПРОС ДЛЯ ПОЛУЧЕНИЯ РАЗМЕРОВ ВСЕХ ТАБЛИЦ]

-- 3. Статистика использования
-- [ЗАПРОС ДЛЯ pg_stat_user_tables]
```

### Задание 5.2: Найдите потенциальные проблемы

Используя скрипты мониторинга, проверьте:

1. ✅ Есть ли dead rows в таблицах?
2. ✅ Равномерно ли распределены данные?
3. ✅ Есть ли долго выполняющиеся запросы?

## Критерии оценки

### Отлично (90-100 баллов):
- ✅ Правильно выбраны стратегии распределения для всех таблиц
- ✅ Все запросы выполняются быстро (в пределах указанного времени)
- ✅ Проведен полный анализ планов выполнения
- ✅ Выполнены эксперименты с альтернативными стратегиями
- ✅ Сделаны обоснованные выводы

### Хорошо (75-89 баллов):
- ✅ Выбраны правильные стратегии для основных таблиц
- ✅ Большинство запросов работают эффективно
- ✅ Проведен базовый анализ производительности
- ✅ Выполнены основные эксперименты

### Удовлетворительно (60-74 балла):
- ✅ Созданы все таблицы (даже если distribution не оптимален)
- ✅ Запросы выполняются (даже если медленно)
- ✅ Проведена базовая проверка данных

## Отчет о выполнении

Подготовьте краткий отчет (1-2 страницы), включающий:

1. **Решения по схеме данных:**
   - Выбранные стратегии распределения
   - Обоснование выбора

2. **Результаты запросов:**
   - Время выполнения каждого запроса
   - Найденные узкие места

3. **Эксперименты:**
   - Результаты сравнения стратегий
   - Графики (если есть)

4. **Выводы и рекомендации:**
   - Что сработало хорошо
   - Что можно улучшить
   - Рекомендации для production

## Дополнительные задания (бонусные баллы)

### Задание 6.1: Партиционирование (10 баллов)

Создайте партиционированную версию таблицы orders по кварталам и сравните производительность для запросов по датам.

### Задание 6.2: Индексы (10 баллов)

Добавьте индексы на часто запрашиваемые поля и измерьте улучшение производительности.

### Задание 6.3: Материализованное представление (10 баллов)

Создайте материализованное представление для часто выполняемого аналитического запроса.

## Полезные команды

```sql
-- Время выполнения
\timing on

-- Просмотр плана без выполнения
EXPLAIN ...

-- Просмотр плана с реальным выполнением
EXPLAIN ANALYZE ...

-- Обновление статистики
ANALYZE table_name;

-- Размер таблицы
SELECT pg_size_pretty(pg_total_relation_size('table_name'));

-- Распределение данных
SELECT gp_segment_id, count(*) FROM table_name GROUP BY gp_segment_id;
```

## Сдача работы

Предоставьте:
1. SQL скрипты с вашими решениями
2. Отчет в формате PDF или Markdown
3. Скриншоты результатов EXPLAIN ANALYZE (опционально)

Удачи! 🚀
